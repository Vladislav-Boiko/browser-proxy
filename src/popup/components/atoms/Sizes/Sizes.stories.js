import React from 'react';

import './Sizes.css';
export default {
  title: 'Sizes',
  component: null,
};

const Template = () => <div></div>;

export const Story = Template.bind({});
const tree = {};
Story.args = tree;
